<nav class="navbar navbar-default">
  <div class="container">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
    </div>
    <div class="collapse navbar-collapse" id="navbar-collapse">
      <ul class="navbar-nav navbar-left">
        <% unless client_signed_in? %>
          <%= link_to 'OpenWalletKG', root_path, class: 'navbar-brand' %>
        <% else %>
          <%= link_to 'OpenWalletKG', show_wallet_path, class: 'navbar-brand' %>
        <% end %>
      </ul>
      <ul class="navbar-nav navbar-right">
      <% unless client_signed_in? %>
        <%= link_to 'Регистрация', new_client_registration_path, class: 'btn btn-default navbar-btn' %>
        <%= link_to 'Вход', new_client_session_path, class: 'btn btn-default navbar-btn' %>
      <% else %>
        <%= link_to 'Выход', destroy_client_session_path, class: 'btn btn-default navbar-btn' %>
      <% end %>
      </ul>
    </div>
  </div>
</nav>